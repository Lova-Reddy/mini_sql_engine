# Mini SQL Engine

A simplified in-memory SQL query engine built in Python.

## Setup Instructions
1. Ensure Python 3 is installed.
2. Clone the repository.
3. Run the CLI:
   ```bash
   python cli.py